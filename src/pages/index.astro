---
import BaseHead from '../components/BaseHead.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import { SITE_TITLE, SITE_DESCRIPTION } from '../consts';
---
<!DOCTYPE html>
<html lang="en">
<head>
  <BaseHead title={SITE_TITLE} description={SITE_DESCRIPTION} />
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800;900&family=JetBrains+Mono:wght@400;500&display=swap" rel="stylesheet">
</head>
<body>

  <!-- Fixed particle canvas background -->
  <canvas id="bg-canvas"></canvas>

  <!-- Dark header overlay -->
  <Header />

  <main>

    <!-- HERO: pure animation viewport, NO text on top -->
    <section class="hero">
      <!-- Minimal HUD — bottom corners only -->
      <div class="hud-corner hud-tl">
        <span class="hud-mono">ALICE · Pb+Pb · √s=13 TeV</span>
      </div>
      <div class="hud-corner hud-tr">
        <span class="hud-mono">N₀ tracks: <span id="track-count">0</span></span>
      </div>
      <div class="hud-corner hud-br">
        <span class="hud-mono hint-pulse">↓ scroll to explore</span>
      </div>
      <div class="hud-corner hud-bl">
        <span class="hud-mono hint-click">⊕ click to collide</span>
      </div>

      <!-- Particle legend — right side -->
      <div class="hud-legend">
        <div class="legend-title">PARTICLES</div>
        <div class="legend-item"><span class="legend-dot" style="background:#00e5ff"></span><span class="legend-name">Electron</span></div>
        <div class="legend-item"><span class="legend-dot" style="background:#ff7043"></span><span class="legend-name">Pion</span></div>
        <div class="legend-item"><span class="legend-dot" style="background:#ce93d8"></span><span class="legend-name">Muon</span></div>
        <div class="legend-item"><span class="legend-dot" style="background:#66bb6a"></span><span class="legend-name">Kaon</span></div>
        <div class="legend-item"><span class="legend-dot" style="background:#ffee58"></span><span class="legend-name">Photon</span></div>
        <div class="legend-item"><span class="legend-dot" style="background:#ef5350"></span><span class="legend-name">Proton</span></div>
      </div>
    </section>

    <!-- PARALLAX CONTENT: slides up over the fixed canvas -->
    <div class="content-area">

      <!-- INTRO CARD — first thing visible on scroll -->
      <section class="intro-section">
        <div class="section-inner">
          <div class="badge">
            <span class="badge-dot"></span>
            ALICE Experiment · CERN · Münster
          </div>
          <h1>Hi, I'm<br><span class="name">Madhva Fakare</span></h1>
          <p class="tagline">MSc Physics student building machine learning pipelines for electron-pion separation using the Transition Radiation Detector at CERN.</p>
          <div class="hero-actions">
            <a href="/assistant" class="btn-primary">
              <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="12" cy="12" r="3"/><path d="M12 1v4M12 19v4M4.22 4.22l2.83 2.83M16.95 16.95l2.83 2.83M1 12h4M19 12h4M4.22 19.78l2.83-2.83M16.95 7.05l2.83-2.83"/></svg>
              Physics AI Assistant
            </a>
            <a href="https://github.com/madhva8" target="_blank" class="btn-secondary">
              <svg width="16" height="16" viewBox="0 0 16 16" fill="currentColor"><path d="M8 0C3.58 0 0 3.58 0 8c0 3.54 2.29 6.53 5.47 7.59.4.07.55-.17.55-.38 0-.19-.01-.82-.01-1.49-2.01.37-2.53-.49-2.69-.94-.09-.23-.48-.94-.82-1.13-.28-.15-.68-.52-.01-.53.63-.01 1.08.58 1.23.82.72 1.21 1.87.87 2.33.66.07-.52.28-.87.51-1.07-1.78-.2-3.64-.89-3.64-3.95 0-.87.31-1.59.82-2.15-.08-.2-.36-1.02.08-2.12 0 0 .67-.21 2.2.82.64-.18 1.32-.27 2-.27.68 0 1.36.09 2 .27 1.53-1.04 2.2-.82 2.2-.82.44 1.1.16 1.92.08 2.12.51.56.82 1.27.82 2.15 0 3.07-1.87 3.75-3.65 3.95.29.25.54.73.54 1.48 0 1.07-.01 1.93-.01 2.2 0 .21.15.46.55.38A8.013 8.013 0 0016 8c0-4.42-3.58-8-8-8z"/></svg>
              GitHub
            </a>
          </div>
        </div>
      </section>

      <section class="section">
        <div class="section-inner">
          <div class="section-label">Current Research</div>
          <h2>TRD Electron-Pion Separation</h2>
          <div class="research-grid">
            <div class="research-card main-card">
              <div class="card-top">
                <div class="card-icon">🔬</div>
                <div>
                  <div class="card-title">ALICE Experiment · CERN</div>
                  <div class="card-sub">Run 3 Data Analysis · lxplus servers</div>
                </div>
                <a href="https://github.com/madhva8/alice-trd-analysis" target="_blank" class="github-btn">
                  <svg width="14" height="14" viewBox="0 0 16 16" fill="currentColor"><path d="M8 0C3.58 0 0 3.58 0 8c0 3.54 2.29 6.53 5.47 7.59.4.07.55-.17.55-.38 0-.19-.01-.82-.01-1.49-2.01.37-2.53-.49-2.69-.94-.09-.23-.48-.94-.82-1.13-.28-.15-.68-.52-.01-.53.63-.01 1.08.58 1.23.82.72 1.21 1.87.87 2.33.66.07-.52.28-.87.51-1.07-1.78-.2-3.64-.89-3.64-3.95 0-.87.31-1.59.82-2.15-.08-.2-.36-1.02.08-2.12 0 0 .67-.21 2.2.82.64-.18 1.32-.27 2-.27.68 0 1.36.09 2 .27 1.53-1.04 2.2-.82 2.2-.82.44 1.1.16 1.92.08 2.12.51.56.82 1.27.82 2.15 0 3.07-1.87 3.75-3.65 3.95.29.25.54.73.54 1.48 0 1.07-.01 1.93-.01 2.2 0 .21.15.46.55.38A8.013 8.013 0 0016 8c0-4.42-3.58-8-8-8z"/></svg>
                  Repo
                </a>
              </div>
              <p class="card-desc">Developing an analysis pipeline for particle identification using the ALICE Transition Radiation Detector. Working with O2Physics, ROOT framework, and real Run 3 experimental data on CERN lxplus servers.</p>
              <div class="tags">
                <span>ROOT</span><span>O2Physics</span><span>C++</span>
                <span>BDT / ML</span><span>CERN lxplus</span><span>Run 3</span>
              </div>
            </div>
            <div class="stat-cards">
              <div class="stat-card">
                <div class="stat-num">Run 3</div>
                <div class="stat-label">ALICE Data</div>
              </div>
              <div class="stat-card">
                <div class="stat-num">TRD</div>
                <div class="stat-label">Detector</div>
              </div>
              <div class="stat-card">
                <div class="stat-num">BDT</div>
                <div class="stat-label">ML Method</div>
              </div>
              <div class="stat-card">
                <div class="stat-num">e/π</div>
                <div class="stat-label">Separation</div>
              </div>
            </div>
          </div>
        </div>
      </section>

      <section class="section alt">
        <div class="section-inner">
          <div class="section-label">About Me</div>
          <h2>Who I Am</h2>

          <div class="about-bio">
            <div class="about-text-block">
              <p>I'm a Master's student in Physics at the <strong>University of Münster</strong>, originally from India. My thesis sits at the intersection of experimental particle physics and machine learning — I'm building a pipeline to separate electrons from pions using the Transition Radiation Detector inside the ALICE experiment at CERN.</p>
              <p>What draws me to this problem is the scale of it: heavy-ion collisions at the LHC recreate conditions from microseconds after the Big Bang, and the TRD is one of the key detectors helping us understand what comes out. Getting the particle identification right matters for the physics.</p>
              <p>Day-to-day, I work with real Run 3 data on CERN's <strong>lxplus</strong> servers, writing analysis code in the O2Physics framework, building ROOT macros, and training Boosted Decision Trees to distinguish particle signatures from raw detector signals.</p>
            </div>

            <div class="about-cards">
              <div class="about-card">
                <div class="about-card-icon">🎓</div>
                <div>
                  <div class="about-card-title">Education</div>
                  <div class="about-card-body">MSc Physics<br><span class="muted">University of Münster, Germany</span></div>
                </div>
              </div>
              <div class="about-card">
                <div class="about-card-icon">⚛️</div>
                <div>
                  <div class="about-card-title">Experiment</div>
                  <div class="about-card-body">ALICE · LHC · CERN<br><span class="muted">Run 3 data · Pb+Pb collisions</span></div>
                </div>
              </div>
              <div class="about-card">
                <div class="about-card-icon">🔬</div>
                <div>
                  <div class="about-card-title">Focus</div>
                  <div class="about-card-body">Electron-pion separation<br><span class="muted">TRD · Boosted Decision Trees</span></div>
                </div>
              </div>
              <div class="about-card">
                <div class="about-card-icon">💻</div>
                <div>
                  <div class="about-card-title">Tools</div>
                  <div class="about-card-body">O2Physics · ROOT · C++<br><span class="muted">Python · Git · lxplus / CVMFS</span></div>
                </div>
              </div>
            </div>
          </div>
        </div>
      </section>

      <section class="section">
        <div class="section-inner">
          <div class="section-label">Connect</div>
          <h2>Find Me</h2>
          <div class="link-grid">
            <a href="https://github.com/madhva8" target="_blank" class="link-card">
              <div class="link-icon-wrap">
                <svg viewBox="0 0 16 16" width="22" height="22" fill="currentColor"><path d="M8 0C3.58 0 0 3.58 0 8c0 3.54 2.29 6.53 5.47 7.59.4.07.55-.17.55-.38 0-.19-.01-.82-.01-1.49-2.01.37-2.53-.49-2.69-.94-.09-.23-.48-.94-.82-1.13-.28-.15-.68-.52-.01-.53.63-.01 1.08.58 1.23.82.72 1.21 1.87.87 2.33.66.07-.52.28-.87.51-1.07-1.78-.2-3.64-.89-3.64-3.95 0-.87.31-1.59.82-2.15-.08-.2-.36-1.02.08-2.12 0 0 .67-.21 2.2.82.64-.18 1.32-.27 2-.27.68 0 1.36.09 2 .27 1.53-1.04 2.2-.82 2.2-.82.44 1.1.16 1.92.08 2.12.51.56.82 1.27.82 2.15 0 3.07-1.87 3.75-3.65 3.95.29.25.54.73.54 1.48 0 1.07-.01 1.93-.01 2.2 0 .21.15.46.55.38A8.013 8.013 0 0016 8c0-4.42-3.58-8-8-8z"/></svg>
              </div>
              <div class="link-info">
                <div class="link-title">GitHub</div>
                <div class="link-sub">alice-trd-analysis</div>
              </div>
              <span class="link-arr">→</span>
            </a>
            <a href="mailto:madhva.fakare@cern.ch" class="link-card">
              <div class="link-icon-wrap">📧</div>
              <div class="link-info">
                <div class="link-title">Email</div>
                <div class="link-sub">madhva.fakare@cern.ch</div>
              </div>
              <span class="link-arr">→</span>
            </a>
            <a href="/blog" class="link-card">
              <div class="link-icon-wrap">📝</div>
              <div class="link-info">
                <div class="link-title">Research Blog</div>
                <div class="link-sub">Notes & updates</div>
              </div>
              <span class="link-arr">→</span>
            </a>
            <a href="/assistant" class="link-card featured">
              <div class="link-icon-wrap">⚛</div>
              <div class="link-info">
                <div class="link-title">Physics AI Assistant</div>
                <div class="link-sub">Powered by Gemini & DeepSeek</div>
              </div>
              <span class="link-arr">→</span>
            </a>
          </div>
        </div>
      </section>

    </div><!-- /content-area -->
  </main>

  <Footer />

</body>
</html>

<script is:inline>
(function () {
  const canvas = document.getElementById('bg-canvas');
  const ctx = canvas.getContext('2d');
  let W, H, cx, cy, R;

  // ── Offscreen canvas for the static detector geometry ──
  const detCanvas = document.createElement('canvas');
  const dctx = detCanvas.getContext('2d');
  let detDirty = true; // rebuild when resized

  // ── State declared early so resize() can safely reference them ──
  let tracks = [], hits = [], beams = [], glow = 0, autoT = 0;
  const trackEl = document.getElementById('track-count');

  function resize() {
    W = canvas.width  = window.innerWidth;
    H = canvas.height = window.innerHeight;
    cx = W/2; cy = H/2;
    R  = Math.min(W, H) * 0.46;
    detCanvas.width  = W;
    detCanvas.height = H;
    detDirty = true;
    // Safe to reset now — variables are always initialised before this runs
    tracks = []; hits = []; beams = [];
    // Restart cycle with updated dimensions so beams use correct cx/cy/W
    if(typeof startCycle==='function') setTimeout(startCycle, 0);
  }
  resize();
  window.addEventListener('resize', ()=>{ resize(); });

  // ── Detector radii as fractions of R ──
  const D = {
    beamPipe:   0.034,
    itsInner:   0.062,
    itsOuter:   0.148,
    tpcInner:   0.165,
    tpcOuter:   0.530,
    trdInner:   0.550,
    trdOuter:   0.710,
    tofInner:   0.720,
    tofOuter:   0.780,
    emcalInner: 0.800,
    emcalOuter: 0.930,
  };
  const N_TRD = 18;

  // ── Colours ──
  const C = {
    electron: '#00e5ff',
    pion:     '#ff7043',
    muon:     '#ce93d8',
    kaon:     '#66bb6a',
    photon:   '#ffee58',
    proton:   '#ef5350',
  };
  const cArr = Object.values(C);
  const rC = () => cArr[Math.floor(Math.random()*cArr.length)];

  // Pre-parsed RGB components for each colour (avoid per-frame hex parsing)
  const CRGB = {};
  for (const [k,v] of Object.entries(C)) {
    CRGB[k] = [parseInt(v.slice(1,3),16), parseInt(v.slice(3,5),16), parseInt(v.slice(5,7),16)];
  }
  function rgba(hex, a) {
    const r=parseInt(hex.slice(1,3),16), g=parseInt(hex.slice(3,5),16), b=parseInt(hex.slice(5,7),16);
    return `rgba(${r},${g},${b},${a.toFixed(2)})`;
  }

  // ─────────────────────────────────────────────
  //  BUILD DETECTOR — drawn once to offscreen canvas
  // ─────────────────────────────────────────────
  function buildDetector() {
    const c = dctx;
    c.clearRect(0,0,W,H);
    const ri = f => f * R;

    // Background
    c.fillStyle = 'rgb(2,5,14)';
    c.fillRect(0,0,W,H);

    // ── TPC bulk fill ──
    c.beginPath();
    c.arc(cx,cy,ri(D.tpcOuter),0,Math.PI*2);
    c.arc(cx,cy,ri(D.tpcInner),0,Math.PI*2,true);
    c.fillStyle='rgba(4,18,38,0.60)'; c.fill();

    // TPC pad rows — batch ALL into one path per colour to save ctx calls
    c.beginPath();
    const tpcStep = 16; // fewer rings, still looks great
    for (let p=0; p<=tpcStep; p++) {
      const r2 = ri(D.tpcInner) + (ri(D.tpcOuter)-ri(D.tpcInner))*p/tpcStep;
      c.moveTo(cx+r2, cy); c.arc(cx,cy,r2,0,Math.PI*2);
    }
    c.strokeStyle='rgba(0,140,220,0.07)'; c.lineWidth=0.6; c.stroke();

    // TPC radial sectors — single path
    c.beginPath();
    for (let s=0; s<18; s++) {
      const a=Math.PI*2*s/18;
      c.moveTo(cx+ri(D.tpcInner)*Math.cos(a), cy+ri(D.tpcInner)*Math.sin(a));
      c.lineTo(cx+ri(D.tpcOuter)*Math.cos(a), cy+ri(D.tpcOuter)*Math.sin(a));
    }
    c.strokeStyle='rgba(0,140,220,0.05)'; c.lineWidth=0.5; c.stroke();

    // TPC boundary
    c.beginPath(); c.arc(cx,cy,ri(D.tpcOuter),0,Math.PI*2);
    c.strokeStyle='rgba(0,170,255,0.28)'; c.lineWidth=1.2; c.stroke();
    c.beginPath(); c.arc(cx,cy,ri(D.tpcInner),0,Math.PI*2);
    c.strokeStyle='rgba(0,170,255,0.18)'; c.lineWidth=0.8; c.stroke();

    // ── ITS layers ──
    c.beginPath();
    c.arc(cx,cy,ri(D.itsOuter),0,Math.PI*2);
    c.arc(cx,cy,ri(D.itsInner),0,Math.PI*2,true);
    c.fillStyle='rgba(0,28,52,0.45)'; c.fill();
    [0.067,0.082,0.098,0.114,0.128,0.140,0.148].forEach((f,i) => {
      c.beginPath(); c.arc(cx,cy,ri(f),0,Math.PI*2);
      c.strokeStyle=`rgba(0,210,255,${(0.30-i*0.028).toFixed(2)})`; c.lineWidth=1; c.stroke();
    });

    // ── TRD supermodules (18 × 6 layers) ──
    const trdR0=ri(D.trdInner), trdR1=ri(D.trdOuter);
    const trdFills=['rgba(0,38,18,0.70)','rgba(0,48,22,0.70)','rgba(0,58,28,0.70)',
                    'rgba(0,68,33,0.70)','rgba(0,78,38,0.70)','rgba(0,88,43,0.70)'];
    // Draw all cells of same layer colour in one pass
    for (let lay=0; lay<6; lay++) {
      c.beginPath();
      for (let sm=0; sm<N_TRD; sm++) {
        const a0=Math.PI*2*sm/N_TRD, a1=Math.PI*2*(sm+1)/N_TRD;
        const r0=trdR0+(trdR1-trdR0)*lay/6, r1=trdR0+(trdR1-trdR0)*(lay+1)/6;
        c.arc(cx,cy,r1,a0,a1); c.arc(cx,cy,r0,a1,a0,true); c.closePath();
      }
      c.fillStyle=trdFills[lay]; c.fill();
      c.strokeStyle='rgba(0,180,70,0.10)'; c.lineWidth=0.4; c.stroke();
    }
    // TRD supermodule walls — single path
    c.beginPath();
    for (let sm=0; sm<N_TRD; sm++) {
      const a=Math.PI*2*sm/N_TRD;
      c.moveTo(cx+trdR0*Math.cos(a), cy+trdR0*Math.sin(a));
      c.lineTo(cx+trdR1*Math.cos(a), cy+trdR1*Math.sin(a));
    }
    c.strokeStyle='rgba(0,220,80,0.28)'; c.lineWidth=0.8; c.stroke();
    // TRD layer rings — single path
    c.beginPath();
    for (let lay=1; lay<6; lay++) {
      const lr=trdR0+(trdR1-trdR0)*lay/6;
      c.moveTo(cx+lr,cy); c.arc(cx,cy,lr,0,Math.PI*2);
    }
    c.strokeStyle='rgba(0,200,70,0.09)'; c.lineWidth=0.5; c.stroke();
    // TRD outer/inner boundary
    c.beginPath(); c.arc(cx,cy,trdR1,0,Math.PI*2);
    c.strokeStyle='rgba(0,220,80,0.55)'; c.lineWidth=1.5; c.stroke();
    c.beginPath(); c.arc(cx,cy,trdR0,0,Math.PI*2);
    c.strokeStyle='rgba(0,220,80,0.35)'; c.lineWidth=1; c.stroke();
    // SM labels
    c.font=`${Math.max(7,R*0.018)}px monospace`;
    c.textAlign='center'; c.textBaseline='middle'; c.fillStyle='rgba(0,220,80,0.32)';
    for (let sm=0; sm<N_TRD; sm+=3) {
      const amid=Math.PI*2*(sm+0.5)/N_TRD, lr=(trdR0+trdR1)/2;
      c.fillText(`SM${sm}`, cx+lr*Math.cos(amid), cy+lr*Math.sin(amid));
    }

    // ── TOF ──
    c.beginPath();
    c.arc(cx,cy,ri(D.tofOuter),0,Math.PI*2);
    c.arc(cx,cy,ri(D.tofInner),0,Math.PI*2,true);
    c.fillStyle='rgba(8,8,48,0.55)'; c.fill();
    c.beginPath();
    for (let s=0; s<18; s++) {
      const a=Math.PI*2*s/18;
      c.moveTo(cx+ri(D.tofInner)*Math.cos(a), cy+ri(D.tofInner)*Math.sin(a));
      c.lineTo(cx+ri(D.tofOuter)*Math.cos(a), cy+ri(D.tofOuter)*Math.sin(a));
    }
    c.strokeStyle='rgba(80,140,255,0.18)'; c.lineWidth=0.7; c.stroke();
    c.beginPath(); c.arc(cx,cy,ri(D.tofOuter),0,Math.PI*2);
    c.strokeStyle='rgba(80,140,255,0.38)'; c.lineWidth=1.2; c.stroke();
    c.beginPath(); c.arc(cx,cy,ri(D.tofInner),0,Math.PI*2);
    c.strokeStyle='rgba(80,140,255,0.22)'; c.lineWidth=0.7; c.stroke();

    // ── EMCal (partial arc) ──
    const emA0=Math.PI*0.82, emA1=Math.PI*1.18;
    c.beginPath();
    c.arc(cx,cy,ri(D.emcalOuter),emA0,emA1);
    c.arc(cx,cy,ri(D.emcalInner),emA1,emA0,true);
    c.closePath();
    c.fillStyle='rgba(38,8,38,0.58)'; c.fill();
    c.strokeStyle='rgba(180,80,220,0.32)'; c.lineWidth=1; c.stroke();
    c.beginPath();
    for (let t=0; t<=14; t++) {
      const a=emA0+(emA1-emA0)*t/14;
      c.moveTo(cx+ri(D.emcalInner)*Math.cos(a), cy+ri(D.emcalInner)*Math.sin(a));
      c.lineTo(cx+ri(D.emcalOuter)*Math.cos(a), cy+ri(D.emcalOuter)*Math.sin(a));
    }
    c.strokeStyle='rgba(180,80,220,0.13)'; c.lineWidth=0.5; c.stroke();

    // ── Beam pipe ──
    c.beginPath(); c.arc(cx,cy,ri(D.beamPipe),0,Math.PI*2);
    c.strokeStyle='rgba(255,80,80,0.55)'; c.lineWidth=2; c.stroke();
    c.beginPath(); c.moveTo(0,cy); c.lineTo(W,cy);
    c.strokeStyle='rgba(255,80,80,0.10)'; c.lineWidth=1.5; c.stroke();
    c.beginPath(); c.arc(cx,cy,ri(0.012),0,Math.PI*2);
    c.fillStyle='rgba(255,120,120,0.35)'; c.fill();
    c.strokeStyle='rgba(255,160,160,0.75)'; c.lineWidth=1.5; c.stroke();

    // ── Labels ──
    const labels=[
      {lbl:'ITS', r:D.itsOuter*0.82,  a:-0.28, col:'rgba(0,210,255,0.55)'},
      {lbl:'TPC', r:(D.tpcInner+D.tpcOuter)/2, a:-0.20, col:'rgba(0,170,255,0.45)'},
      {lbl:'TRD', r:(D.trdInner+D.trdOuter)/2, a:-0.22, col:'rgba(0,220,80,0.65)'},
      {lbl:'TOF', r:(D.tofInner+D.tofOuter)/2, a:-0.24, col:'rgba(80,140,255,0.58)'},
      {lbl:'EMCal',r:(D.emcalInner+D.emcalOuter)/2, a:Math.PI*0.99, col:'rgba(180,80,220,0.55)'},
    ];
    c.font=`bold ${Math.max(10,R*0.032)}px JetBrains Mono, monospace`;
    c.textBaseline='middle';
    labels.forEach(l => {
      const lx=cx+l.r*R*Math.cos(l.a)+6, ly=cy+l.r*R*Math.sin(l.a);
      c.textAlign=(l.lbl==='EMCal')?'center':'left';
      c.fillStyle=l.col;
      c.fillText(l.lbl, lx, ly);
    });
    c.textAlign='left'; c.textBaseline='alphabetic';
  }

  // ─────────────────────────────────────────────
  //  TRACKS — pre-computed arcs, revealed over time
  // ─────────────────────────────────────────────
  function makeTrack(angle) {
    const t = Math.random();
    let color, maxR, curv, dashed=false, species;

    // ── Physics-accurate detector response per particle species ──
    //
    // ELECTRON  (t<0.12): High curvature (light, charged). Spirals in B-field,
    //   leaves hits in ITS+TPC+TRD, stopped/showered in EMCal.
    //
    // PION      (t<0.42): Most abundant (~80% of tracks). Moderate curvature.
    //   Leaves hits through ITS→TPC→TRD→TOF. Hadronic interaction stops in TRD/TOF.
    //
    // MUON      (t<0.52): Minimum-ionising particle. Tiny curvature, NOT stopped
    //   by calorimeters. Punches through the entire detector and exits — only
    //   particle that escapes the detector completely.
    //
    // KAON      (t<0.64): Heavier than pion, shorter range. Stops in TRD.
    //   Decays in flight → bent track.
    //
    // PHOTON    (t<0.74): No charge → straight line, NO track in TPC/TRD.
    //   Converts to e+e- pair in ITS or starts EM shower in EMCal.
    //   Shown as dashed straight line to EMCal inner boundary.
    //
    // PROTON    (t<1.00): Heaviest, most curved (high dE/dx). Stops in TPC
    //   or early TRD depending on momentum.

    if (t < 0.12) {
      species='electron'; color=C.electron;
      // ELECTRON — ITS → TPC → TRD → EMCal
      // Bent by 0.5T B-field. At typical momenta (1–5 GeV/c) the track curves
      // visibly but still traverses all inner subdetectors. Showered & absorbed
      // in EMCal. TRD gives strong e/π discrimination via transition radiation.
      // Curvature: moderate so arc visibly crosses TPC and TRD layers.
      maxR = (D.emcalInner + 0.005) * R;
      curv = (Math.random()>0.5?1:-1) * (0.004 + Math.random()*0.004);

    } else if (t < 0.42) {
      species='pion'; color=C.pion;
      // PION — ITS → TPC → TRD → TOF
      // Most abundant particle (~80% of charged tracks in Pb+Pb).
      // Hadronic, so NOT stopped by EMCal. Identified at TOF by time-of-flight.
      // Slightly less curved than electrons at same momentum.
      // Stops inside TOF detector volume.
      maxR = (D.tofInner + Math.random()*(D.tofOuter-D.tofInner+0.03)) * R;
      curv = (Math.random()>0.5?1:-1) * (0.002 + Math.random()*0.004);

    } else if (t < 0.52) {
      species='muon'; color=C.muon;
      // MUON — ITS → TPC → TRD → TOF → exits detector completely
      // Minimum-ionising: not stopped by EMCal or any hadronic absorber in
      // the central barrel. Punches through everything and leaves the detector.
      // (ALICE has a dedicated forward muon spectrometer but central-barrel
      //  muons simply escape.) Tiny curvature — near-straight track.
      maxR = 1.6 * R;
      curv = (Math.random()>0.5?1:-1) * (0.001 + Math.random()*0.002);

    } else if (t < 0.64) {
      species='kaon'; color=C.kaon;
      // KAON — ITS → TPC → TRD → TOF
      // Heavier than pion (494 MeV/c² vs 140 MeV/c²) so more curved at same
      // momentum. Identified at TOF — kaon arrives later than pion for same p.
      // Short-lived kaons (K±) reach TOF; K0s decay before TPC outer edge.
      // More curved than pion, but still reaches TOF.
      maxR = (D.tofInner + Math.random()*(D.tofOuter-D.tofInner)) * R;
      curv = (Math.random()>0.5?1:-1) * (0.006 + Math.random()*0.006);

    } else if (t < 0.74) {
      species='photon'; color=C.photon;
      // PHOTON — straight to EMCal (no charge → no B-field deflection)
      // Invisible in ITS/TPC/TRD (no ionisation). Converts to e+e- pair at
      // EMCal inner face and produces an electromagnetic shower.
      // Shown as a dashed line — no ionisation track, just direction.
      maxR = (D.emcalInner + 0.005) * R;
      curv = 0; dashed = true;

    } else {
      species='proton'; color=C.proton;
      // PROTON — ITS → TPC → TRD → TOF
      // Heaviest stable hadron (938 MeV/c²). Most curved of the charged hadrons
      // at same momentum. At LHC momenta (>1 GeV/c) protons DO reach TOF and
      // are identified there — they arrive latest of all species.
      // Highly curved track, clearly distinguished from pions.
      maxR = (D.tofInner + Math.random()*(D.tofOuter-D.tofInner)) * R;
      curv = (Math.random()>0.5?1:-1) * (0.009 + Math.random()*0.008);
    }

    const step = 1.5;
    const pts = [];
    let vx=Math.cos(angle)*step, vy=Math.sin(angle)*step;
    let px=cx, py=cy;
    let prevDistSq=0;
    const maxIter = species==='muon' ? 1200 : species==='electron' ? 1000 : 700;

    while (pts.length < maxIter) {
      if (curv !== 0) {
        const spd = Math.sqrt(vx*vx+vy*vy);
        const a2 = Math.atan2(vy,vx) + curv;
        vx = Math.cos(a2)*spd; vy = Math.sin(a2)*spd;
      }
      px += vx; py += vy;
      const distSq = (px-cx)**2 + (py-cy)**2;
      if (distSq >= maxR*maxR) break;
      // Muons: also break if they exit the canvas
      if (px<-20||px>W+20||py<-20||py>H+20) break;
      // Stop only if track loops back past origin (unphysical at these momenta)
      // maxR is the primary stop condition; this catches extreme spiral cases
      if (species!=='electron' && species!=='muon' && pts.length>20 && distSq < (D.itsOuter*R)**2) break;
      prevDistSq = distSq;
      pts.push(px, py);
    }
    return {pts, color, drawTo:0, width:0.9+Math.random()*0.8, dashed, species};
  }

  // ─────────────────────────────────────────────
  //  HITS
  // ─────────────────────────────────────────────
  function spawnHits(angle, color, species) {
    const ri = f => f * R;
    const jit = s => angle + (Math.random()-0.5)*s;

    // ── ITS: all charged particles leave hits in silicon layers ──
    if (species !== 'photon') {
      // ITS has 6 layers in Run 2 (2 SPD + 2 SDD + 2 SSD), shown as 4 here
      [0.068, 0.085, 0.110, 0.142].forEach(f => {
        if (Math.random() > 0.20) hits.push({type:'ITS', r:ri(f), a:jit(0.04), color, age:0, lt:300});
      });
    }

    // ── TPC: continuous ionisation clusters, all charged particles ──
    // TPC has up to 159 pad rows — we show 10 representative clusters
    if (species !== 'photon') {
      for (let p=0; p<10; p++) {
        const fr = D.tpcInner + (D.tpcOuter-D.tpcInner)*p/10;
        hits.push({type:'TPC', r:ri(fr), a:jit(0.06), color, age:0, lt:300});
      }
    }

    // ── TRD: 6 layers for electrons, pions, muons, kaons, protons ──
    // ALL hadrons and leptons that reach TRD leave tracklets in all 6 layers.
    // (Photons leave nothing — they are neutral.)
    // TRD is the key detector for your thesis: transition radiation is only
    // produced by ultra-relativistic electrons, not by pions at same momentum.
    if (species !== 'photon') {
      for (let lay=0; lay<6; lay++) {
        const fr = D.trdInner + (D.trdOuter-D.trdInner)*(lay+0.5)/6;
        hits.push({type:'TRD', r:ri(fr), a:jit(0.020), age:0, lt:400, lay});
      }
    }

    // ── TOF: all charged hadrons and muons that survive to TOF ──
    // Pions, kaons, protons, muons all reach TOF and produce a hit.
    // Electrons are absorbed in EMCal before reaching TOF.
    // TOF identifies particles by measuring arrival time (Δt ~ mass²/p²).
    if (species==='pion' || species==='kaon' || species==='proton' || species==='muon') {
      hits.push({type:'TOF', r:ri((D.tofInner+D.tofOuter)/2), a:jit(0.015), age:0, lt:450});
    }
  }

  function drawHits() {
    // Draw all same-type hits together to minimise state changes
    const trd=[],tof=[],tpc=[],its=[];
    hits=hits.filter(h=>{ h.age++; return h.age<h.lt; });
    for(const h of hits){
      if(h.type==='TRD') trd.push(h);
      else if(h.type==='TOF') tof.push(h);
      else if(h.type==='TPC') tpc.push(h);
      else its.push(h);
    }
    // TPC dots — all same colour to batch
    for(const h of tpc){
      const fade=1-h.age/h.lt;
      const hx=cx+h.r*Math.cos(h.a), hy=cy+h.r*Math.sin(h.a);
      ctx.beginPath(); ctx.arc(hx,hy,R*0.004,0,Math.PI*2);
      ctx.fillStyle=rgba(h.color, fade*0.55); ctx.fill();
    }
    // ITS squares
    for(const h of its){
      const fade=1-h.age/h.lt, s=R*0.009;
      const hx=cx+h.r*Math.cos(h.a), hy=cy+h.r*Math.sin(h.a);
      ctx.fillStyle=rgba(h.color, fade*0.85);
      ctx.fillRect(hx-s/2,hy-s/2,s,s);
    }
    // TRD tracklets — use transform without save/restore to preserve clip
    for(const h of trd){
      const fade=1-h.age/h.lt;
      const hx=cx+h.r*Math.cos(h.a), hy=cy+h.r*Math.sin(h.a);
      const hw=R*0.020, hh=R*0.009;
      const ang=h.a+Math.PI/2;
      const cos=Math.cos(ang), sin=Math.sin(ang);
      // Draw rotated rect manually as a parallelogram
      const dx1=cos*hw/2, dy1=sin*hw/2;
      const dx2=-sin*hh/2, dy2=cos*hh/2;
      ctx.beginPath();
      ctx.moveTo(hx+dx1+dx2, hy+dy1+dy2);
      ctx.lineTo(hx-dx1+dx2, hy-dy1+dy2);
      ctx.lineTo(hx-dx1-dx2, hy-dy1-dy2);
      ctx.lineTo(hx+dx1-dx2, hy+dy1-dy2);
      ctx.closePath();
      ctx.fillStyle=`rgba(0,255,128,${(fade*0.92).toFixed(2)})`;
      ctx.fill();
    }
    // TOF circles
    for(const h of tof){
      const fade=1-h.age/h.lt;
      const hx=cx+h.r*Math.cos(h.a), hy=cy+h.r*Math.sin(h.a);
      ctx.beginPath(); ctx.arc(hx,hy,R*0.009,0,Math.PI*2);
      ctx.fillStyle=`rgba(120,190,255,${(fade*0.90).toFixed(2)})`; ctx.fill();
    }
  }

  // ─────────────────────────────────────────────
  //  CYCLE STATE MACHINE
  //  idle → beams → collision → hold → fade → idle
  // ─────────────────────────────────────────────
  let phase = 'idle';   // idle | beams | collision | hold | fade
  let holdAlpha = 1.0;  // opacity during fade
  let cycleTimer = null;

  function clearCycleTimer() {
    if(cycleTimer) { clearTimeout(cycleTimer); cycleTimer=null; }
  }

  function startCycle() {
    clearCycleTimer();
    tracks=[]; hits=[]; beams=[]; holdAlpha=1.0;
    phase='beams';
    spawnBeams();
  }

  function triggerCollision() {
    clearCycleTimer();
    glow=1; beams=[];
    phase='collision'; // tracks grow outward, loop watches for completion
    const n = 44 + Math.floor(Math.random()*14);
    for(let i=0;i<n;i++){
      const angle = Math.PI*2*i/n + (Math.random()-0.5)*0.28;
      const tr = makeTrack(angle);
      tracks.push(tr);
      spawnHits(angle, tr.color, tr.species);
    }
    // hold + fade scheduled by loop once all tracks finish drawing
  }

  function spawnBeams() {
    beams=[];
    const bY = cy;
    beams.push({x:-W*0.1,  y:bY,   vx:7,  trail:[], alpha:1.0,  r:2.8});
    beams.push({x:-W*0.1,  y:bY-3, vx:7,  trail:[], alpha:0.35, r:1.4});
    beams.push({x:-W*0.1,  y:bY+3, vx:7,  trail:[], alpha:0.35, r:1.4});
    beams.push({x:W*1.1,   y:bY,   vx:-7, trail:[], alpha:1.0,  r:2.8});
    beams.push({x:W*1.1,   y:bY-3, vx:-7, trail:[], alpha:0.35, r:1.4});
    beams.push({x:W*1.1,   y:bY+3, vx:-7, trail:[], alpha:0.35, r:1.4});
  }

  // Click/tap always restarts immediately
  canvas.addEventListener('click',()=>startCycle());
  canvas.addEventListener('touchend',e=>{e.preventDefault();startCycle();},{passive:false});

  // ─────────────────────────────────────────────
  //  MAIN LOOP
  // ─────────────────────────────────────────────
  let hidden=false;
  document.addEventListener('visibilitychange',()=>{ hidden=document.hidden; });

  function loop() {
    requestAnimationFrame(loop);
    if(hidden) return; // don't burn CPU when tab not visible

    // Rebuild detector onto offscreen canvas only when needed
    if(detDirty){ buildDetector(); detDirty=false; }

    // Clear full canvas, then stamp detector
    ctx.clearRect(0, 0, W, H);
    ctx.drawImage(detCanvas, 0, 0);

    // Collision flash — no radialGradient every frame unless active
    if(glow>0){
      const gr=ctx.createRadialGradient(cx,cy,0,cx,cy,R*0.35);
      gr.addColorStop(0,`rgba(255,245,150,${(glow*0.60).toFixed(2)})`);
      gr.addColorStop(0.5,`rgba(80,160,255,${(glow*0.15).toFixed(2)})`);
      gr.addColorStop(1,'transparent');
      ctx.fillStyle=gr; ctx.fillRect(0,0,W,H);
      glow-=0.028;
      if(glow<0)glow=0;
    }

    // ── Pb beams drawn BEFORE clip so trails span full canvas width ──
    beams.forEach(b=>{
      b.trail.push({x:b.x, y:b.y});
      if(b.trail.length>28) b.trail.shift();
      b.x += b.vx;
    });
    // Collision check: main beams (alpha=1) meet at IP
    const mainL = beams.find(b=>b.vx>0 && b.alpha===1.0);
    const mainR = beams.find(b=>b.vx<0 && b.alpha===1.0);
    if(mainL && mainR && mainL.x >= mainR.x){
      triggerCollision(); beams=[];
    } else {
      ctx.save(); // save state for beam drawing — no clip active
      beams.forEach(b=>{
        if(b.trail.length < 2) return;
        // Glowing trail: gradient from transparent at tail to bright at head
        const tailX = b.trail[0].x, tipX = b.x;
        const grad = ctx.createLinearGradient(tailX, b.y, tipX, b.y);
        grad.addColorStop(0, 'rgba(255,60,80,0)');
        grad.addColorStop(0.6, `rgba(255,60,80,${(b.alpha*0.4).toFixed(2)})`);
        grad.addColorStop(1, `rgba(255,180,120,${b.alpha.toFixed(2)})`);
        ctx.beginPath();
        ctx.moveTo(b.trail[0].x, b.trail[0].y);
        b.trail.forEach(pt => ctx.lineTo(pt.x, pt.y));
        ctx.strokeStyle = grad;
        ctx.lineWidth = b.r * 0.8;
        ctx.globalAlpha = 1;
        ctx.stroke();
        // Bright leading dot (only for main beams)
        if(b.alpha === 1.0){
          ctx.beginPath(); ctx.arc(b.x, b.y, b.r, 0, Math.PI*2);
          ctx.fillStyle = 'rgba(255,120,80,0.95)'; ctx.fill();
          // Soft glow around dot
          const grd = ctx.createRadialGradient(b.x,b.y,0, b.x,b.y, b.r*4);
          grd.addColorStop(0,'rgba(255,80,50,0.5)');
          grd.addColorStop(1,'rgba(255,80,50,0)');
          ctx.beginPath(); ctx.arc(b.x,b.y,b.r*4,0,Math.PI*2);
          ctx.fillStyle=grd; ctx.fill();
        }
      });
      ctx.restore();
      beams = beams.filter(b => b.x > -W*0.15 && b.x < W*1.15);
    }

    // ── Draw muon tracks BEFORE clip (they exit the detector) ──
    // ── All other tracks/hits drawn AFTER clip ──
    // ── Advance tracks during collision phase ──
    // Speed: 3px/frame at step=1.5 → 2 pts/frame → ~60fps means
    // a pion reaching tofOuter (~200pts) takes ~3s. Feels natural.
    if(phase==='collision'){
      let allDone = true;
      tracks.forEach(tr=>{
        tr.drawTo = Math.min(tr.pts.length, tr.drawTo + 3);
        if(tr.drawTo < tr.pts.length) allDone = false;
      });
      if(allDone && !cycleTimer){
        // All tracks fully drawn — hold 5s then fade
        cycleTimer = setTimeout(()=>{ phase='fade'; }, 5000);
      }
    }

    // ── Draw muons UNCLIPPED first (they exit the detector) ──
    tracks.filter(tr=>tr.species==='muon').forEach(tr=>renderTrack(tr));

    // ── Clip all other drawing to detector boundary ──
    ctx.save();
    ctx.beginPath();
    ctx.arc(cx, cy, D.emcalOuter * R, 0, Math.PI * 2);
    ctx.clip('nonzero');

    drawHits();

    // Draw all non-muon tracks inside the clip
    tracks.filter(tr=>tr.species!=='muon').forEach(tr=>renderTrack(tr));

    if(phase==='idle'){ tracks=[]; hits=[]; }

    ctx.restore(); // end clip region

    // ── renderTrack: draw full track from IP to stopping point ──
    function renderTrack(tr) {
      const end = Math.min(tr.drawTo, tr.pts.length);
      if(end < 4) return;

      if(tr.dashed){
        // Photon: straight dashed line, no ionisation
        ctx.setLineDash([5,5]);
        ctx.beginPath();
        ctx.moveTo(tr.pts[0], tr.pts[1]);
        for(let i=2; i<end; i+=2) ctx.lineTo(tr.pts[i], tr.pts[i+1]);
        ctx.strokeStyle=tr.color; ctx.lineWidth=1.0;
        ctx.globalAlpha=0.80; ctx.stroke();
        ctx.setLineDash([]); ctx.globalAlpha=1;
        return;
      }

      // ── Full trail from IP to current tip — always unbroken ──
      ctx.beginPath();
      ctx.moveTo(tr.pts[0], tr.pts[1]);
      for(let i=2; i<end; i+=2) ctx.lineTo(tr.pts[i], tr.pts[i+1]);
      ctx.strokeStyle = tr.color;
      ctx.lineWidth   = tr.width * 0.9;
      ctx.globalAlpha = 0.85;
      ctx.stroke();

      // ── Bright dot at current tip ──
      ctx.beginPath();
      ctx.arc(tr.pts[end-2], tr.pts[end-1], tr.width*2.2, 0, Math.PI*2);
      ctx.fillStyle = tr.color; ctx.globalAlpha = 1.0; ctx.fill();

      ctx.globalAlpha = 1;
    }
    if(trackEl) trackEl.textContent=tracks.length;

    // ── Fade phase: reduce global alpha on all tracks ──
    if(phase==='fade'){
      holdAlpha = Math.max(0, holdAlpha-0.012); // ~1.4s fade
      // Apply fade by re-drawing a dark overlay
      ctx.save();
      ctx.globalAlpha = 0.06;
      ctx.fillStyle='rgb(2,5,14)';
      ctx.fillRect(0,0,W,H);
      ctx.restore();
      if(holdAlpha<=0){
        phase='idle';
        tracks=[]; hits=[]; beams=[]; holdAlpha=1.0;
        // Pause 2s then start next cycle
        cycleTimer = setTimeout(startCycle, 2000);
      }
    }
  }

  // Kick off first cycle
  cycleTimer = setTimeout(startCycle, 800);
  loop();
})();
</script>

<style>
  *, *::before, *::after { box-sizing: border-box; }

  :root {
    --accent: #00d4ff;
    --accent2: #6366f1;
    --white: #ffffff;
  }

  html, body {
    margin: 0; padding: 0;
    font-family: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
    background: #04060e;
    color: #111;
    overflow-x: hidden;
  }

  /* Canvas fills full viewport, fixed behind everything */
  #bg-canvas {
    position: fixed;
    top: 0; left: 0;
    width: 100vw; height: 100vh;
    z-index: 0;
    pointer-events: auto;
  }

  /* Header sits on top of canvas */
  header {
    position: relative;
    z-index: 20;
    background: rgba(4,6,14,0.7) !important;
    backdrop-filter: blur(16px) !important;
    -webkit-backdrop-filter: blur(16px) !important;
    border-bottom: 1px solid rgba(255,255,255,0.06) !important;
    box-shadow: none !important;
  }
  header :global(a),
  header :global(.brand-name),
  header :global(.logo-text) {
    color: rgba(255,255,255,0.9) !important;
  }
  header :global(a:hover) {
    color: #fff !important;
    background: rgba(255,255,255,0.08) !important;
  }
  header :global(a.active) {
    color: var(--accent) !important;
    background: rgba(0,212,255,0.1) !important;
  }
  header :global(.hamburger span) {
    background: rgba(255,255,255,0.8) !important;
  }
  header :global(.nav-links) {
    background: rgba(4,6,14,0.95) !important;
  }

  main { position: relative; z-index: 10; }

  /* ── HERO: pure full-viewport animation window, NO text ── */
  .hero {
    position: relative;
    height: 100svh;
    pointer-events: none;
    overflow: hidden;
  }

  /* Corner HUD labels — minimal, unobtrusive */
  .hud-corner {
    position: absolute;
    font-family: 'JetBrains Mono', monospace;
    pointer-events: none;
  }
  .hud-tl { top: 72px; left: 16px; }
  .hud-tr { top: 72px; right: 16px; text-align: right; }
  .hud-bl { bottom: 20px; left: 16px; }
  .hud-br { bottom: 20px; right: 16px; text-align: right; }
  .hud-mono {
    font-size: 0.65rem;
    color: rgba(0,212,255,0.4);
    letter-spacing: 0.06em;
    text-transform: uppercase;
  }
  @keyframes hintPulse {
    0%,100% { opacity: 0.3; }
    50% { opacity: 0.7; }
  }
  .hint-pulse { animation: hintPulse 2.5s ease-in-out infinite; }
  .hint-click { animation: hintPulse 2s ease-in-out infinite 0.5s; }

  /* ── Particle legend ── */
  .hud-legend {
    position: absolute;
    right: 16px;
    top: 50%;
    transform: translateY(-50%);
    pointer-events: none;
    display: flex;
    flex-direction: column;
    gap: 7px;
  }
  .legend-title {
    font-family: 'JetBrains Mono', monospace;
    font-size: 0.58rem;
    letter-spacing: 0.12em;
    color: rgba(0,212,255,0.35);
    text-transform: uppercase;
    margin-bottom: 4px;
    text-align: right;
  }
  .legend-item {
    display: flex;
    align-items: center;
    justify-content: flex-end;
    gap: 7px;
  }
  .legend-dot {
    width: 7px;
    height: 7px;
    border-radius: 50%;
    flex-shrink: 0;
    opacity: 0.75;
    order: 2;
  }
  .legend-name {
    font-family: 'JetBrains Mono', monospace;
    font-size: 0.60rem;
    letter-spacing: 0.06em;
    color: rgba(200,230,255,0.45);
    text-transform: uppercase;
    order: 1;
  }

  /* ── CONTENT AREA: slides up over the fixed canvas (parallax) ── */
  .content-area {
    position: relative;
    z-index: 10;
    background: #f4f6fa;
    border-radius: 28px 28px 0 0;
    box-shadow: 0 -8px 60px rgba(0,0,0,0.55), 0 -1px 0 rgba(255,255,255,0.06);
    overflow: hidden;
    margin-top: -2px; /* tiny overlap so no gap */
  }

  /* ── INTRO SECTION — first card in scroll area ── */
  .intro-section {
    padding: 4rem 1.5rem 3rem;
    border-bottom: 1px solid #e8e8e8;
    background: linear-gradient(160deg, #fff 0%, #f4f6fa 100%);
  }
  .intro-section .section-inner {
    max-width: 700px;
    margin: 0 auto;
  }

  .badge {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    background: #e8f4ff;
    border: 1px solid rgba(0,112,243,0.2);
    color: #0070f3;
    font-size: 0.73rem;
    font-weight: 600;
    padding: 6px 14px;
    border-radius: 20px;
    margin-bottom: 1.4rem;
    letter-spacing: 0.03em;
    text-transform: uppercase;
  }
  .badge-dot {
    width: 7px; height: 7px;
    background: #0070f3;
    border-radius: 50%;
    box-shadow: 0 0 6px #0070f3;
    animation: pulse-dot 2s infinite;
  }
  @keyframes pulse-dot {
    0%,100% { opacity:1; transform:scale(1); }
    50%      { opacity:0.5; transform:scale(0.8); }
  }

  h1 {
    font-size: clamp(2.4rem, 5.5vw, 3.8rem);
    font-weight: 900;
    line-height: 1.1;
    margin: 0 0 1rem;
    letter-spacing: -0.03em;
    color: #0a0a0a;
  }
  .name {
    background: linear-gradient(135deg, #0070f3, #7928ca);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .tagline {
    font-size: clamp(0.95rem, 1.8vw, 1.1rem);
    color: #555;
    line-height: 1.7;
    margin: 0 0 2rem;
    max-width: 560px;
  }

  .hero-actions {
    display: flex;
    gap: 12px;
    flex-wrap: wrap;
  }
  .btn-primary {
    display: inline-flex; align-items: center; gap: 8px;
    background: linear-gradient(135deg, #0070f3, #7928ca);
    color: white; padding: 13px 26px; border-radius: 12px;
    text-decoration: none; font-size: 0.95rem; font-weight: 600;
    transition: all 0.2s; box-shadow: 0 4px 20px rgba(0,112,243,0.28);
  }
  .btn-primary:hover { transform: translateY(-2px); box-shadow: 0 8px 28px rgba(0,112,243,0.4); }
  .btn-secondary {
    display: inline-flex; align-items: center; gap: 8px;
    background: white; color: #333; padding: 13px 26px; border-radius: 12px;
    text-decoration: none; font-size: 0.95rem; font-weight: 600;
    border: 1px solid #ddd; transition: all 0.2s;
  }
  .btn-secondary:hover { background: #f5f5f5; border-color: #ccc; }

  .section {
    padding: 3.5rem 1.5rem;
    border-bottom: 1px solid #e8e8e8;
  }
  .section:last-child { border-bottom: none; }
  .section.alt { background: #fff; }

  .section-inner {
    max-width: 860px;
    margin: 0 auto;
  }

  .section-label {
    font-size: 0.7rem;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.12em;
    color: #0070f3;
    margin-bottom: 6px;
  }

  h2 {
    font-size: clamp(1.4rem, 3vw, 2rem);
    font-weight: 800;
    color: #0a0a0a;
    margin: 0 0 1.8rem;
    letter-spacing: -0.02em;
  }

  /* Research grid */
  .research-grid {
    display: grid;
    grid-template-columns: 1fr 180px;
    gap: 16px;
    align-items: start;
  }
  @media (max-width: 640px) {
    .research-grid { grid-template-columns: 1fr; }
  }

  .research-card {
    background: white;
    border: 1px solid #e8e8e8;
    border-radius: 16px;
    padding: 1.5rem;
    box-shadow: 0 2px 16px rgba(0,0,0,0.05);
  }
  .card-top {
    display: flex;
    align-items: flex-start;
    gap: 12px;
    margin-bottom: 1rem;
    flex-wrap: wrap;
  }
  .card-icon { font-size: 1.8rem; flex-shrink: 0; }
  .card-title { font-size: 0.92rem; font-weight: 700; color: #111; }
  .card-sub { font-size: 0.75rem; color: #888; margin-top: 2px; }
  .github-btn {
    margin-left: auto;
    display: inline-flex; align-items: center; gap: 5px;
    padding: 5px 12px; background: #111; color: white;
    border-radius: 8px; text-decoration: none; font-size: 0.75rem; font-weight: 500;
    transition: background 0.15s; flex-shrink: 0;
  }
  .github-btn:hover { background: #333; }
  .card-desc { font-size: 0.88rem; color: #555; line-height: 1.7; margin: 0 0 1rem; }
  .tags { display: flex; flex-wrap: wrap; gap: 6px; }
  .tags span { background: #f0f4ff; color: #0050cc; padding: 4px 10px; border-radius: 20px; font-size: 0.73rem; font-weight: 500; }

  .stat-cards { display: grid; grid-template-columns: 1fr 1fr; gap: 10px; }
  .stat-card {
    background: white;
    border: 1px solid #e8e8e8;
    border-radius: 12px;
    padding: 1rem 0.75rem;
    text-align: center;
    box-shadow: 0 2px 8px rgba(0,0,0,0.04);
  }
  .stat-num { font-size: 1.4rem; font-weight: 800; color: #0a0a0a; letter-spacing: -0.02em; }
  .stat-label { font-size: 0.68rem; color: #999; margin-top: 3px; text-transform: uppercase; letter-spacing: 0.06em; }

  /* About */
  .about-bio {
    display: grid;
    grid-template-columns: 1fr 260px;
    gap: 2.5rem;
    align-items: start;
  }
  @media (max-width: 720px) { .about-bio { grid-template-columns: 1fr; } }

  .about-text-block p {
    font-size: 0.97rem;
    color: #444;
    line-height: 1.80;
    margin: 0 0 1rem;
  }
  .about-text-block p:last-child { margin-bottom: 0; }
  .about-text-block strong { color: #111; }

  .about-cards {
    display: flex;
    flex-direction: column;
    gap: 10px;
  }
  .about-card {
    display: flex;
    align-items: flex-start;
    gap: 12px;
    background: white;
    border: 1px solid #eaeaea;
    border-radius: 12px;
    padding: 12px 14px;
    box-shadow: 0 1px 6px rgba(0,0,0,0.04);
  }
  .about-card-icon { font-size: 1.2rem; flex-shrink: 0; margin-top: 1px; }
  .about-card-title { font-size: 0.75rem; font-weight: 700; color: #0070f3; text-transform: uppercase; letter-spacing: 0.06em; margin-bottom: 3px; }
  .about-card-body { font-size: 0.85rem; color: #222; line-height: 1.5; }
  .muted { color: #888; font-size: 0.78rem; }

  /* Link grid */
  .link-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 12px; }
  @media (max-width: 480px) { .link-grid { grid-template-columns: 1fr; } }

  .link-card {
    display: flex;
    align-items: center;
    gap: 14px;
    padding: 16px 18px;
    background: white;
    border: 1px solid #e8e8e8;
    border-radius: 14px;
    text-decoration: none;
    color: #333;
    transition: all 0.15s;
    box-shadow: 0 2px 8px rgba(0,0,0,0.04);
  }
  .link-card:hover { border-color: #0070f3; transform: translateY(-2px); box-shadow: 0 6px 20px rgba(0,112,243,0.12); }
  .link-card.featured { background: linear-gradient(135deg, #f0f4ff, #f5f0ff); border-color: #c8d8ff; }
  .link-card.featured:hover { border-color: var(--accent2); box-shadow: 0 6px 20px rgba(99,102,241,0.15); }
  .link-icon-wrap { font-size: 1.3rem; flex-shrink: 0; display: flex; align-items: center; justify-content: center; width: 36px; height: 36px; background: #f5f5f5; border-radius: 8px; }
  .link-card.featured .link-icon-wrap { background: rgba(99,102,241,0.1); }
  .link-info { flex: 1; }
  .link-title { font-size: 0.88rem; font-weight: 600; color: #111; }
  .link-sub { font-size: 0.73rem; color: #888; margin-top: 2px; }
  .link-arr { color: #ccc; font-size: 1rem; }
  .link-card:hover .link-arr { color: #0070f3; }
  .link-card.featured:hover .link-arr { color: var(--accent2); }

  /* Footer dark theme */
  footer {
    position: relative;
    z-index: 10;
    background: #0a0a0f !important;
    color: rgba(255,255,255,0.4) !important;
    border-top: 1px solid rgba(255,255,255,0.06) !important;
  }
  footer :global(*) { color: rgba(255,255,255,0.4) !important; }

  /* Mobile tweaks */
  @media (max-width: 500px) {
    .intro-section { padding: 2.5rem 1rem 2rem; }
    h1 { font-size: 2.2rem; }
    .tagline { font-size: 0.92rem; }
    .btn-primary, .btn-secondary { padding: 11px 18px; font-size: 0.88rem; }
    .section { padding: 2.5rem 1rem; }
    .stat-cards { grid-template-columns: repeat(4,1fr); gap: 6px; }
    .stat-num { font-size: 1.1rem; }
    .about-grid { grid-template-columns: 1fr; }
    .link-grid { grid-template-columns: 1fr; }
    .hud-tl, .hud-tr { top: 64px; }
    .hud-mono { font-size: 0.58rem; }

    /* Legend moves to bottom-center on mobile, horizontal layout */
    .hud-legend {
      top: auto;
      bottom: 48px;
      right: 50%;
      transform: translateX(50%);
      flex-direction: row;
      flex-wrap: wrap;
      justify-content: center;
      gap: 8px 14px;
      background: rgba(0,0,0,0.35);
      border-radius: 8px;
      padding: 6px 10px;
    }
    .legend-title { display: none; }
    .legend-item { justify-content: center; }
    .legend-name { font-size: 0.52rem; }
    .legend-dot { width: 6px; height: 6px; order: 1; }
    .legend-name { order: 2; }
  }
</style>
